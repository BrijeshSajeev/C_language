#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int s, v, u, a;
    cout << "\nEnter v , u , a";
    cin >> v >> u >> a;
    s = (pow(v, 2) - pow(u, s)) / (2 * a);
    cout << "\nSpeed = " << s;
    return 0;
    
}