#include <iostream>
using namespace std;
int main()
{
    int b, h, a;
    cout << "\nEnter base ::> ";
    cin >> b;
    cout << "\nEnter height ::> ";
    cin >> h;
    a = (b * h) / 2;
    cout << "area = " << a;
}